import { FilteredCardsPipe } from './filtered-cards.pipe';

describe('FilteredCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
